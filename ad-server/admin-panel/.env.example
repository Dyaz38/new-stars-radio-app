# Build-time: public API base URL (no trailing slash on /api/v1 — include full path)
# Local backend:
# VITE_API_BASE_URL=http://localhost:8000/api/v1
#
# Production (Railway) — replace with your deployed API URL:
# VITE_API_BASE_URL=https://your-service.up.railway.app/api/v1

VITE_API_BASE_URL=http://localhost:8000/api/v1
