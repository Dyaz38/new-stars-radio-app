# Public API root (no trailing slash). Listener + ad banner use this.
# VITE_AD_SERVER_URL=https://your-railway-host.up.railway.app/api/v1

# Optional: Google AdSense fallback when your ad-server has no banner (404 / error / image fail).
# Create a Display ad unit in AdSense and use its slot id. Publisher id must start with ca-pub-.
# VITE_ADSENSE_PUBLISHER_ID=ca-pub-xxxxxxxxxxxxxxxx
# VITE_ADSENSE_BANNER_SLOT=1234567890
